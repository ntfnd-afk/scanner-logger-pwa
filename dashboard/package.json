{
  "name": "scanner-dashboard",
  "version": "1.0.0",
  "description": "Dashboard для мониторинга сканирования",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node -e \"console.log('\\n✅ Build complete! Run deploy.ps1 or deploy.sh to upload to server')\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.0"
  }
}

